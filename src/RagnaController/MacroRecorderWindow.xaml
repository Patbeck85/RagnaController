<Window x:Class="RagnaController.MacroRecorderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Macro Recorder" Width="700" Height="550"
        Background="{StaticResource BG1Brush}"
        Foreground="{StaticResource TextHiBrush}"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="MACRO RECORDER" FontSize="20" FontWeight="Bold"
                       Foreground="{StaticResource NeonBrush}"
                       Effect="{StaticResource NeonGlow}"/>
            <TextBlock Text="Record button sequences with timing, then bind to any button"
                       FontSize="11" Foreground="{StaticResource TextLowBrush}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Controls -->
        <Border Grid.Row="1" Background="{StaticResource BG2Brush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Recording Status -->
                <StackPanel Grid.Column="0" Orientation="Horizontal"
                            VerticalAlignment="Center">
                    <Ellipse x:Name="RecordingDot" Width="12" Height="12"
                             Fill="#FF3A52" Margin="0,0,10,0"/>
                    <TextBlock x:Name="StatusText" Text="Ready to Record"
                               FontSize="14" FontWeight="Bold"/>
                    <TextBlock x:Name="StepCountText" Text="(0 steps)"
                               FontSize="11" Foreground="{StaticResource TextLowBrush}"
                               Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Record Controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="BtnRecord" Content="● Record"
                            Style="{StaticResource ConsoleDangerBtn}"
                            Width="100" Margin="0,0,10,0"
                            Click="BtnRecord_Click"/>
                    <Button x:Name="BtnStop" Content="■ Stop"
                            Style="{StaticResource ConsoleGhostBtn}"
                            Width="80" IsEnabled="False"
                            Click="BtnStop_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Step List -->
        <Border Grid.Row="2" Background="{StaticResource BG2Brush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- List Header -->
                <Border Grid.Row="0" Background="{StaticResource BG3Brush}"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,0,0,1" Padding="15,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="80"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="#" FontWeight="Bold"
                                   Foreground="{StaticResource NeonDimBrush}"/>
                        <TextBlock Grid.Column="1" Text="ACTION" FontWeight="Bold"
                                   Foreground="{StaticResource NeonDimBrush}"/>
                        <TextBlock Grid.Column="2" Text="DELAY (ms)" FontWeight="Bold"
                                   Foreground="{StaticResource NeonDimBrush}"/>
                        <TextBlock Grid.Column="3" Text="" FontWeight="Bold"
                                   Foreground="{StaticResource NeonDimBrush}"/>
                    </Grid>
                </Border>

                <!-- Step Items -->
                <ListBox x:Name="StepsList" Grid.Row="1"
                         Background="Transparent" BorderThickness="0"
                         Padding="10" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{StaticResource BorderBrush}"
                                                BorderThickness="0,0,0,1" Padding="5">
                                            <ContentPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <!-- Empty State -->
                <StackPanel x:Name="EmptyState" Grid.Row="1"
                            VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="No steps recorded yet" FontSize="14"
                               Foreground="{StaticResource TextLowBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Press Record and perform your combo"
                               FontSize="11" Foreground="{StaticResource TextLowBrush}"
                               HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Footer Actions -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Macro Name -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Macro Name:" VerticalAlignment="Center"
                           Margin="0,0,10,0" Foreground="{StaticResource TextLowBrush}"/>
                <TextBox x:Name="MacroNameText" Width="200" Height="32"
                         Padding="8,6" Text="Untitled Macro"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Save Macro" Style="{StaticResource ConsolePrimaryBtn}"
                        Width="120" Margin="0,0,10,0"
                        Click="BtnSave_Click" IsEnabled="False"
                        x:Name="BtnSave"/>
                <Button Content="Clear" Style="{StaticResource ConsoleDangerBtn}"
                        Width="80" Margin="0,0,10,0"
                        Click="BtnClear_Click"/>
                <Button Content="Close" Style="{StaticResource ConsoleGhostBtn}"
                        Width="80" Click="BtnClose_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
